---
import EventPageLayout from "@/components/EventPageLayout.astro";
import { eventSeries } from "@/data/eventSeries";

const data = eventSeries["demo-nights"];
---

<EventPageLayout
  name={data.name}
  slug={data.slug}
  logo={data.logo}
  description={data.description}
  lumaUrl={data.lumaUrl}
  lumaCalendarId={data.lumaCalendarId}
/>
